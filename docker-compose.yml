version: '3.8'

services:
  # ========================================
  # 1. Redis
  # ========================================
  redis:
    image: redis:alpine
    container_name: evolution_redis
    command: redis-server --appendonly yes --requirepass 123456
    volumes:
      - ./evolution_data/redis:/data
    networks:
      - voice_sdr_network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "123456", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ========================================
  # 2. Postgres
  # ========================================
  postgres:
    image: postgres:15
    container_name: evolution_postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=evolution
      - POSTGRES_PASSWORD=evolution
      - POSTGRES_DB=evolution
    volumes:
      - ./evolution_data/postgres:/var/lib/postgresql/data
    networks:
      - voice_sdr_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evolution"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s

  # ========================================
  # 3. Evolution API v2 (Blindada)
  # ========================================
  evolution-api:
    image: atendai/evolution-api:v2.1.1
    container_name: evolution_whatsapp
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    
    # --- FIX 1: DNS Google (Obrigatório) ---
    dns:
      - 8.8.8.8
      - 8.8.4.4
    
    ports:
      - "8080:8080"
    
    environment:
      - SERVER_URL=http://localhost:8080
      - DOCKER_ENV=true
      - AUTHENTICATION_API_KEY=123456
      
      # Redis
      - CACHE_REDIS_ENABLED=true
      - CACHE_REDIS_URI=redis://:123456@redis:6379/0
      
      # Banco
      - DATABASE_ENABLED=true
      - DATABASE_PROVIDER=postgresql
      - DATABASE_CONNECTION_URI=postgresql://evolution:evolution@postgres:5432/evolution
      - DATABASE_SAVE_DATA_INSTANCE=true
      - DATABASE_SAVE_DATA_NEW_MESSAGE=true
      
      # Webhooks
      - WEBHOOK_GLOBAL_ENABLED=true
      - WEBHOOK_GLOBAL_URL=http://sdr-bot:8000/webhook/evolution
      - WEBHOOK_GLOBAL_WEBHOOK_BY_EVENTS=true
      - WEBHOOK_EVENTS_QRCODE_UPDATED=true
      - WEBHOOK_EVENTS_MESSAGES_UPSERT=true
      - WEBHOOK_EVENTS_CONNECTION_UPDATE=true
      
      # Configs Extras
      - LOG_LEVEL=INFO
      - DEL_INSTANCE=false
      
      # Tuning para evitar timeout
      - QRCODE_LIMIT=30
      - WEBHOOK_EVENTS_APPLICATION_STARTUP=false
    
    volumes:
      - ./evolution_data/instances:/evolution/instances
      - ./evolution_data/store:/evolution/store
    
    networks:
      - voice_sdr_network

  # ========================================
  # 4. Voice SDR Bot
  # ========================================
  sdr-bot:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: voice_sdr_bot
    restart: unless-stopped
    depends_on:
      - evolution-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - EVOLUTION_API_URL=http://evolution-api:8080
      - EVOLUTION_API_KEY=123456
    volumes:
      - ./app:/app/app:ro
    networks:
      - voice_sdr_network
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

# ========================================
# FIX 2: Correção de MTU para Rede
# ========================================
networks:
  voice_sdr_network:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1400  # Reduz tamanho do pacote para passar na rede